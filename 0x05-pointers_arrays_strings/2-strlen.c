#include "main.h"
/**
 * _strlen - gives the length of a given string
 * @s: is the pointer to string of characters
 * Return: number of characters
 */
int _strlen(char *s)
{
	int count;

	for (count = 0; s[count] != '\0'; count++)
	{
	}
	return (count);
}
